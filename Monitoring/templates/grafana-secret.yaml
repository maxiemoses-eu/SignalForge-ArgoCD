apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin-credentials
  namespace: monitoring # Ensure this matches where you deploy the app
type: Opaque
stringData:
  # These are the actual values Grafana will use
  admin-user: admin
  admin-password: your-really-secure-password