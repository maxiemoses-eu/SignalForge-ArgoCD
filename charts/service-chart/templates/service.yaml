apiVersion: v1
kind: Service
metadata:
  name: {{ include "order.fullname" . }}
  labels:
    {{- include "order.labels" . | nindent 4 }}

spec:
  type: ClusterIP
  selector:
    {{- include "order.selectorLabels" . | nindent 4 }}

  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
