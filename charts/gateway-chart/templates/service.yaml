apiVersion: v1
kind: Service
metadata:
  name: signalforge-gateway
spec:
  ports:
    - port: 80        # Public port (e.g., inside the cluster)
      targetPort: 9999 # The port our App is actually listening on
  selector:
    app: signalforge-gateway